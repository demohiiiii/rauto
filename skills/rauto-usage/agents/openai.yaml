interface:
  display_name: "Rauto Operator"
  short_description: "Run rauto directly: execute, query, replay, tx, backup, restore"
  default_prompt: "Use $rauto-usage to run rauto directly for me (exec/template/tx/workflow/replay/backup), return key results, and ask only for missing required inputs."
