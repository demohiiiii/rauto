interface:
  display_name: "Rauto Operator"
  short_description: "Install from releases, verify, and run rauto"
  default_prompt: "Use $rauto-usage to install rauto from GitHub Releases, verify it, or run rauto directly for me, and ask only for missing required inputs."
