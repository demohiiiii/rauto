interface:
  display_name: "Rauto Operations"
  short_description: "Run safe rauto show, tx, Web, and Agent flows"
  default_prompt: "Use $rauto-usage to operate rauto safely: prefer show for reads, tx/workflow/orchestrate for config changes, and validate CLI/Web/Agent workflows."
